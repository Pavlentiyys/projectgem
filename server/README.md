# Server - Express TypeScript с Gemini AI

Backend сервер для чат-приложения с интеграцией Google Gemini AI.

## 📋 Требования

- Node.js (версия 18 или выше)
- Yarn (менеджер пакетов)
- API ключ от Google Gemini AI

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
yarn install
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне директории `server/`:

```env
PORT=3000
CLIENT_URL=http://localhost:5173
GEMINI_API_KEY=your_gemini_api_key_here
```

**Где получить GEMINI_API_KEY:**
1. Перейдите на [Google AI Studio](https://makersuite.google.com/app/apikey)
2. Создайте новый API ключ
3. Скопируйте ключ в файл `.env`

### 3. Запуск в режиме разработки

```bash
yarn dev
```

Сервер запустится на `http://localhost:3000` с автоматической перезагрузкой при изменениях.

### 4. Сборка проекта

```bash
yarn build
```

Скомпилированные файлы будут находиться в директории `dist/`.

### 5. Запуск в production режиме

```bash
yarn build
yarn start
```

## 📡 API Endpoints

### POST `/api/messages/send`

Отправляет сообщение в Gemini AI и возвращает ответ.

**Request Body:**
```json
{
  "message": "Привет, как дела?",
  "history": [
    {
      "type": "user",
      "content": "Предыдущее сообщение пользователя"
    },
    {
      "type": "ai",
      "content": "Предыдущий ответ AI"
    }
  ]
}
```

**Response:**
```json
{
  "userMessage": {
    "type": "user",
    "content": "Привет, как дела?"
  },
  "aiMessage": {
    "type": "ai",
    "content": "Ответ от Gemini AI"
  }
}
```

**Параметры:**
- `message` (string, обязательный) - текст сообщения пользователя
- `history` (array, опциональный) - история предыдущих сообщений для контекста

### GET `/health`

Проверка работоспособности сервера.

**Response:**
```json
{
  "status": "ok",
  "timestamp": "2026-01-31T12:00:00.000Z"
}
```

## 🏗️ Архитектура проекта

Проект использует MVC архитектуру с ООП подходом:

```
server/
├── src/
│   ├── models/
│   │   └── Message.ts          # Модель сообщения (тип и содержание)
│   ├── services/
│   │   └── GeminiService.ts    # Сервис для работы с Gemini API
│   ├── controllers/
│   │   └── MessageController.ts # Контроллер для обработки HTTP запросов
│   └── routes/
│       └── messageRoutes.ts    # Маршруты API
├── server.ts                   # Точка входа сервера
├── package.json
├── tsconfig.json
└── .env                        # Переменные окружения (не коммитится)
```

### Описание компонентов:

- **Models** - Классы данных (Message)
- **Services** - Бизнес-логика (GeminiService для работы с AI)
- **Controllers** - Обработка HTTP запросов и валидация
- **Routes** - Настройка маршрутов Express

## 🔧 Настройка

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `3000` |
| `CLIENT_URL` | URL фронтенд приложения для CORS | `http://localhost:5173` |
| `GEMINI_API_KEY` | API ключ Google Gemini | - |

### Модель Gemini

По умолчанию используется `gemini-1.5-flash`. Для изменения модели отредактируйте файл `src/services/GeminiService.ts`:

```typescript
this.model = this.genAI.getGenerativeModel({ model: 'gemini-1.5-pro' });
```

Доступные модели:
- `gemini-1.5-flash` - быстрая и экономичная (по умолчанию)
- `gemini-1.5-pro` - более мощная модель

## 🛠️ Скрипты

- `yarn dev` - Запуск в режиме разработки с hot-reload
- `yarn build` - Компиляция TypeScript в JavaScript
- `yarn start` - Запуск скомпилированного проекта
- `yarn test` - Запуск тестов (пока не настроено)

## 📝 Логирование

Сервер логирует все входящие запросы в консоль в формате:
```
2026-01-31T12:00:00.000Z - POST /api/messages/send
```

## ⚠️ Обработка ошибок

- Валидация входных данных на уровне контроллера
- Обработка ошибок Gemini API
- Возврат понятных сообщений об ошибках клиенту

## 🔒 Безопасность

- CORS настроен для работы только с указанным CLIENT_URL
- Валидация всех входящих данных
- Обработка ошибок без утечки чувствительной информации

## 📚 Дополнительная информация

- [Google Gemini API Documentation](https://ai.google.dev/gemini-api/docs)
- [Express.js Documentation](https://expressjs.com/)
- [TypeScript Documentation](https://www.typescriptlang.org/)
